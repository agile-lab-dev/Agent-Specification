[project]
name = "agent-specification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openapi-core",
    "PyYAML",
    "requests"
]
